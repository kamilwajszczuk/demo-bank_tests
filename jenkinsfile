pipeline {
    agent any
    environment {
        // Ustawienia środowiska, np. wersja Node.js
        NODE_VERSION = '14' // lub inna wersja, którą używasz
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Run Playwright Tests') {
            steps {
                sh 'npx playwright test'
            }
        }
    }
    post {
        success {
            echo 'Testy zakończyły się sukcesem!'
        }
        failure {
            echo 'Testy nie powiodły się.'
        }
    }
}
