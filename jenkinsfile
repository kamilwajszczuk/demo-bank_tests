pipeline {
   agent { docker { image 'mcr.microsoft.com/playwright:v1.55.1-noble' } }
   stages {
      stage('Deploy') {
            steps {
                sh 'docker build -t playwright-docker .'
                sh 'docker run -it playwright-docker:latest npm run test'
            }
        }
   }
}
